{
  "name": "@tmux-agents/k8s-runtime",
  "version": "0.1.19",
  "description": "Kubernetes runtime for tmux-agents - run agents in K8s pods",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "compile": "npm run build",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["kubernetes", "k8s", "tmux", "agents", "runtime"],
  "author": "super-agent.ai",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/super-agent-ai/tmux-agents.git"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.8.0",
    "vitest": "^2.0.0"
  }
}
