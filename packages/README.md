# Tmux Agents Packages

This directory contains the independently publishable packages:

- **cli/** - Command-line interface client
- **tui/** - Terminal UI client (React + Ink)
- **mcp/** - MCP server for Claude Desktop
- **k8s-runtime/** - Kubernetes runtime support

Each package can be built and published separately.
